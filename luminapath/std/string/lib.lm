use std:list [List]
use std:vec  [Vec]

@![langItem(list as List)]

type string {
  inner [u8]
}

fn toByteVec str as string -> Vec a =
  let len = len str in
  let ptr = alloc len in
    do str.inner . forEach #(ptr:write ptr)
    then { Vec a | ptr, len }
