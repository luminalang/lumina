use std:math [Num [+, -, *, /]]
use std:io
use std:libc [exit]
use maybe [Maybe [Just, Nothing], or, map]
use fileprinter

fn main = fileprinter:main

fn numExit =
  let n = 5 in 
    Just n
    . map #(\n -> n * 2)
    . map #(+ 2)
    . or #50
    . exit
