use std:io

fn add x y (int int -> int)
    x + y

fn magic_number (int) 20

fn main
   io:puts << 
     if magic_number == add 10 10
       then "The numbers are equal"
       else "The numbers are not equal"
